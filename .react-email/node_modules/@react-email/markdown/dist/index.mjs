// src/markdown.tsx
import { parseMarkdownToReactEmailJSX } from "md-to-react-email";
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var Markdown = React.forwardRef(
  ({ children, markdownContainerStyles, markdownCustomStyles, ...props }, forwardedRef) => {
    const parsedMarkdown = parseMarkdownToReactEmailJSX(
      children,
      markdownCustomStyles
    );
    return /* @__PURE__ */ jsx(
      "div",
      {
        ...props,
        ref: forwardedRef,
        "data-id": "react-email-markdown",
        style: markdownContainerStyles,
        dangerouslySetInnerHTML: { __html: parsedMarkdown }
      }
    );
  }
);
Markdown.displayName = "Markdown";
export {
  Markdown
};
